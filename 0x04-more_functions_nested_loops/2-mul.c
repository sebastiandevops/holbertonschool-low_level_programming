/**
 * mul - return the multiplication of two integers
 * @a: Integer to mult.
 * @b: Integer to mult.
 * Return: Result.
 *
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
