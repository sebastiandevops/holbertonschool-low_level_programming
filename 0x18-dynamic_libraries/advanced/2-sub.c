#include "operations.h"
/**
 * mul - return the substraction of two integers
 * @a: Integer to mult.
 * @b: Integer to mult.
 * Return: Result.
 *
 */
int sub(int a, int b)
{
	int result;

	result = (a - b);
	return (result);
}
