#include "operations.h"
/**
 * mul - return the modulo of two integers
 * @a: Integer.
 * @b: Integer.
 * Return: Result.
 *
 */
int mod(int a, int b)
{
	int result;

	result = (a % b);
	return (result);
}
