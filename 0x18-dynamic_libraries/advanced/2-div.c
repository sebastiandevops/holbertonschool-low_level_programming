#include "operations.h"
/**
 * mul - return the division of two integers
 * @a: Integer to mult.
 * @b: Integer to mult.
 * Return: Result.
 *
 */
int div(int a, int b)
{
	int result;

	result = (a / b);
	return (result);
}
