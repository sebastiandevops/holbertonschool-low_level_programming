#include "operations.h"
/**
 * mul - return the sum of two integers
 * @a: Integer to mult.
 * @b: Integer to mult.
 * Return: Result.
 *
 */
int add(int a, int b)
{
	int result;

	result = (a + b);
	return (result);
}
