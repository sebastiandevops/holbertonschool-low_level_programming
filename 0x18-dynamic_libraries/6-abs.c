/**
 * _abs - return 1 if is positive, else 0 or -1
 * @n: The number to check
 *
 * Return: -n if the number is positive.
 * Else: return n.
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
